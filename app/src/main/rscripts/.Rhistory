list_a
#!/bin/Rscript
pdf("generated/pdbcdr3/20_first_tables_histed(no_two_peaks)_bad.pdf")
p <- "generated/pdbcdr3/dist_mats/cdr3+pep"
temp_a <- list.files(path = p, pattern = "*(0).txt")
matlist_a <- list()
for (i in 1:length(temp_a))
{
bufer <- read.table(paste(p, temp_a[i], sep = '/'))
matlist_a[[i]] <- as.matrix(bufer[2:nrow(bufer),][,2:ncol(bufer)])
#hist(matlist[[i]], plot = T)
}
}
}
}
demo()
error.catching
error.catching()
source('~/Projects/vdjstruct/app/src/main/rscripts/calc_koef.R')
